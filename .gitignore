.DS_Store
__pycache__/
node_modules/

# Secrets — never commit
.env
.env.local
.env*.local

# Build output
.next/
.next-dev/
*.tsbuildinfo
next-env.d.ts
bin/
obj/
*.user

# Private notes and internal verification screenshots (not for public repo)
docs/legacy/curriculum/private/
codebase/public/images/faqs/minh-chung-*

# Hackathon data pack — must never be committed (README: "Bảo mật dữ liệu")
/data/

# Personal AI coding-tool config — kept locally, not part of the product
codebase/.agent/
codebase/.agents/
codebase/.claude/
codebase/skills/
codebase/AGENTS.md
codebase/CLAUDE.md
codebase/.cursorrules
codebase/.windsurfrules

# Private interview records with real names — local only
docs/research/private/
