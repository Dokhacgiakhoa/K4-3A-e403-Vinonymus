namespace AIIANotebook.Domain.Enums;

public enum UserRole
{
    Visitor = 0,   // Học viên tự do (Free)
    Member = 1,    // Học viên trả phí (Premium)
    Lecture = 2,   // Giảng viên / Soạn giáo trình
    SuperAdmin = 3 // Quản trị hệ thống
}
