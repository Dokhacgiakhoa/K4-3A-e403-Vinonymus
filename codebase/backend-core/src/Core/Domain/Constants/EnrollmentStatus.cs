namespace AIIANotebook.Domain.Constants;

// Cột course_enrollments.status lưu dạng chuỗi; giữ đúng hai giá trị này để khớp dữ liệu đã có.
public static class EnrollmentStatus
{
    public const string Active = "Active";
    public const string Dropped = "Dropped";
}
